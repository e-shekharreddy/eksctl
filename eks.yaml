# spot-cluster.yaml
apiVersion: eksctl.io/v1alpha5
kind: ClusterConfig

metadata:
  name: roboshop
  region: us-east-1

managedNodeGroups:

# Cheap nodes (Spot)
- name: spot
  instanceTypes:
    - t3.medium
    - t3a.medium
    - m5.large
    - m5a.large
capacityType: SPOT
desiredCapacity: 2
  